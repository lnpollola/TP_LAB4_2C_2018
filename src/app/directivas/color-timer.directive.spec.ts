import { ColorTimerDirective } from './color-timer.directive';

describe('ColorTimerDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorTimerDirective();
    expect(directive).toBeTruthy();
  });
});
