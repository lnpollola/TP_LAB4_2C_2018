import { LetraYcolorDirective } from './letra-ycolor.directive';

describe('LetraYcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new LetraYcolorDirective();
    expect(directive).toBeTruthy();
  });
});
