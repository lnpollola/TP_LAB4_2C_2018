import { EliminoMailPipe } from './elimino-mail.pipe';

describe('EliminoMailPipe', () => {
  it('create an instance', () => {
    const pipe = new EliminoMailPipe();
    expect(pipe).toBeTruthy();
  });
});
