import { AgregoTextoPosPipe } from './agrego-texto-pos.pipe';

describe('AgregoTextoPosPipe', () => {
  it('create an instance', () => {
    const pipe = new AgregoTextoPosPipe();
    expect(pipe).toBeTruthy();
  });
});
