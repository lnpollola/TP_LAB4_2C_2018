import { CambioDolarPipe } from './cambio-dolar.pipe';

describe('CambioDolarPipe', () => {
  it('create an instance', () => {
    const pipe = new CambioDolarPipe();
    expect(pipe).toBeTruthy();
  });
});
